# Node modules (we install fresh in the container)
node_modules

# Local environment files (contain secrets)
.env

# Logs and temporary files
logs
*.log
tmp
dist

# Git files
.git
.gitignore

# Docker cache
.dockerignore